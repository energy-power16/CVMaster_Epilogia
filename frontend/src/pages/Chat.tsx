import { Gradient } from 'components/Gradient/Gradient';
import { ChatWrapper } from 'features/chat';

export const Chat = () => {
    return (
        <>
            <ChatWrapper />
            <Gradient />
        </>
    );
};
